import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles ={
    input: {
        width: "100%",
        backfroungColor: COLORS.light_gray,
        padding: 10,
        borderRadius: 6,
        borderWidth: 1,
        borderColor: COLORS.gray
    },  
    label: {
        marginLeft: 5,
        marginBottom: 4,
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md
    },
}
