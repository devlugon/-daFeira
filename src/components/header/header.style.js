import {COLORS, FONT_SIZE} from "../../constants/theme.js";

export const styles ={
    header: {
        width: "100%",
        alignItems: "center"
    },
    logo: {
        marginTop: 50,
        marginBottom: 10,
    },
    titulo: {
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }
}
